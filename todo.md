# 项目进度记录（TODO / 进展）

> 最后更新：2026-08-06
> 项目：SaaS 需求管理平台 ——「AI 辅助、人机可替换」的需求全生命周期流水线
> 目标用户：一人公司（1 人或几个员工的完整有限公司）+ 中小团队；AI agent 当员工是核心场景

---

## 当前状态

✅ **PRD v0.13 已回填**（v0.6~v0.13 全部定稿决策；v0.13 含需求验收状态机/验收不通过返工/待验收按优先级提醒/提出人线上验收；89 叶子功能，03 已加 §3.1.0 脑图）
⏸ **PRD 尚未最终定稿**——继续走业务评审，未触发技术实现层
⏸ **DDD v0.4 待同步 v0.8~v0.13**（当前停在 v0.7 口径，PRD 定稿后再让架构师对齐）
⏭ **下一步**：PRD 业务评审收尾 → 定稿 → 同步 DDD → M0 技术设计

---

## 已完成

### 团队与技能配置
- `.claude/agents/`：12 个 Agent（11 专家 + Team Lead 队长），含 PM/架构师/前端/UI/UX/后端/DBA/DevOps/SRE/运营/客服
- `.claude/skills/`：92 个技能（mattpocock 22 + PM 技能 70）
- `AGENTS.md`：Leader 调度协议（主会话充当队长，自动按职责表分派专家、汇总交付）

### 产品模型（业务评审中，v0.12）
- **三层需求树**：初始需求（业务描述/目标）→ 产品需求（解决方案）→ 系统需求（技术实现，按业务领域拆）→ 任务（最小交付，分开发/测试两类）；规模自适应可跳过 L1
- **需求层生命周期（v0.10 + v0.13 验收段）**：
  - L1 初始需求：新建 → 澄清中 → 已澄清 → 评估中 → 已评估 → 进行中 → 开发完成 → 待验收 → 已验收
  - L2 产品需求：新建 → 评估中 → 已评估 → PRD → 评审中 → 已通过 → 进行中 → 开发完成 → 待验收 → 已验收
  - L3 系统需求：新建 → 设计 → 评审 → 进行中 → 开发完成 → 待验收 → 已验收（需求层无待发布、中间态统一进行中）
- **验收环节（v0.13）**：完成=开发活动彻底结束（进度100%、状态与进度一致）→ 自动进「待验收」→ 产品经理+提出人并行验收（功能已上线双方可见）→ 对齐敲定 →「已验收」（终态）；验收不通过（产品经理打回/提出人异议经评估）→ 回「进行中」返工重走再验收；待验收提醒按优先级（P0=1天/P1=2天/P2=3天/P3=7天，可配置 NT-02）直到验收敲定；提出人验收他提的整棵需求（提 L1 就 L1 完成后触发，以此类推）、后置不阻塞完成
- **任务双类型（v0.12）**：
  - **开发任务 6 态 + 废弃**：待处理 → 进行中 → 待测试 → 待验收 → 待发布 → 已完成（=发布成功）+ 废弃
  - **测试任务 4 态**：待处理 → 进行中 → 待执行 → 已完成（执行用例 + 三方确认，不发布不验收）
  - 「待测试」仅属开发任务（等测试验证）、「待执行」仅属测试任务（用例设计完成）；任务有「类型」字段
- **测试环节（v0.12）**：技术设计定稿→测试计划→开发并行写用例→开发进「待测试」→测试任务「待执行」执行→测试报告→**三方确认（产品经理+架构师+测试人员）**→测试任务完成→开发进「待验收」；测试计划/用例/报告=本期流程占位（不实现存储）；测试打回累计 3 次升架构师、评审/验收打回累计 3 次挂『强制人工』旁路标记（B4，状态机不变、AI 不代行）、任何打回返工后必须重新测试；发布单只装「测试通过+验收通过」的开发任务；测试任务跟随需求进迭代（不独立排期）、独立估时、继承需求优先级、分派测试角色、计入团队负载
- **L1 评估（v0.10）**：L1 澄清后评估「业务目标值不值得做」→ 做/不做（废弃）门槛（人工专属）；**与 L2 评估（S/M/L 档位）是两套语义**；L1/L2 复用 EV-01~04
- **L3 按 DDD 领域拆分（v0.10）**：L2 拆 L3 按业务领域划分（高内聚低耦合），一个 L3 = 一个业务领域；领域边界 = 技术侧上下文边界
- **完成 = 发布上线**：开发任务「已完成」= 发布成功；需求「完成」= 全部直接子级已完成，系统直接判；发布单**属于迭代**（每单装待发布开发任务，发布单关闭 → 范围内任务自动已完成）；需求本身不进发布单
- **挂起/废弃 → 自动移出发布单（v0.10）**：发布单只发「仍为待发布」任务
- **版本 = 规划维度（v0.9，v0.12 补充）**：版本装需求（可 L1/L2/L3），需求可不归版本（未归=backlog），**归版本由产品经理决定**；已交付=版本下所有需求均已「已验收」（B3，天然不可逆）；需求废弃→自动移出版本、恢复不回版本；挂起→留在版本；父级归版本子级跟随、移出整棵子树移出；需求按版本、任务按迭代；规划交付日期=参考
- **角色体系（v0.12 重构）**：空间级人类角色 7 类（空间负责人/产品经理/架构师/迭代负责人=技术leader/开发人员分栈/测试人员/UI设计师必须）；取消「评审主持人」（L2 产品经理主持、L3 架构师主持）与「验收负责人」（验收归产品经理）；Agent 目录 12 类（含新增测试 Agent）；提出人为成员属性（可见自己需求 L1-3 状态/进度/完成，不可见迭代/任务/成员）；角色 = 权限打包虚拟对象，成员被赋角色获权限
- **UI 设计（v0.12）**：PRD 定稿后即启动（不等评审通过）；UI 设计师产出挂 L2、作 L3 设计输入
- **进度汇总**：系统需求 = 已完成（发布成功）任务/总任务数（待发布/挂起/阻塞/进行中/待处理都算未完成；废弃任务不计分母分子）；父级 = 子级聚合；废弃子级排除；读模型投影不写回
- **人机可替换**：每环节处理人 = 人/Agent/人机协作；团队级模板 + 单需求覆盖；Agent 默认「草稿+确认」、可开全自动；副作用动作默认建议+人确认、可白名单
- **全局标记**：挂起（自选搁置，留档用）/ 阻塞（外部卡住），任意需求类型任意状态可打、带原因、解除回原状态；不沿需求树自动传递，父级显示「下级 N 个阻塞/挂起」风险提醒
- **任务验收打回（v0.7，v0.11 加上限，v0.12 归产品经理）**：待验收被打回 → 回「进行中」+ 必填反馈；打回次数计入任务记录；**累计 3 次验收打回 → 挂『强制人工』旁路标记（B4）**；验收裁决归产品经理（取消验收负责人角色）；**任何打回 → 返工 → 必须重新测试**
- **评审打回 = 回上一环节**（L2→PRD、L3→设计，理由作修订指引）；累计 3 次打回挂『强制人工』旁路标记（B4）；L2 需求评审产品经理主持、L3 技术评审架构师主持
- **直接创建（v0.11 评审必走）**：PM 直接建产品需求（跳澄清与 L1 评估、仍走 L2 评估+评审）；架构师/研发直接建系统需求（技术改进类，跳评估/PRD/评审、带标记，但 L3 设计 → 技术评审**必走**（可轻量不跳过））
- **可开发判定**：系统需求「已拆分」= 全部计划任务已确认创建；拆分接受率低是 AI 拆分质量信号（TA-08）
- **退化完成规则（v0.11）**：废弃子级不计入父级完成判定（不把父级判完成）；全部子级废弃时父级无法自动完成，提示人工处理（父级废弃 / 保留重拆）
- **backlog（v0.11 定名）**：原「待装填池」定名 backlog（待排期需求池）= 已确认但未排期的需求（L2/L3）+ 任务的集合（业务是池子、实现是筛选视图）；挂起任务移出迭代后回到这里
- **废弃级联 + 恢复（v0.8）**：需求废弃（任意阶段）→ 其下未完成任务**级联废弃**（第 6 态，带原因、移出迭代池、不计进度分母）；废弃恢复 → **回废弃前状态快照**（M6，撤销废弃语义），已废弃子级保持废弃可单独恢复、已完成任务保持已完成，带「已恢复」留痕；父级完成判定排除废弃子级；去归档（RE-04 仅删除）
- **多租户 = 公司 → 空间（v0.7）**：租户 = 一个公司；公司下多个空间（原「项目」= 特定需求集目的容器）；账号（公司级，可加入多空间）→ 成员（空间级，绑空间内角色）→ 角色 → 权限；角色在空间下定义、分人类/Agent 两类、可自定义；空间负责人维护空间内角色/权限/账户关系；租户管理员管账号、建空间、定空间负责人
- **目标用户（v0.9）**：核心 = **一人公司**（1 人或几个员工的完整有限公司，AI agent 当员工是核心场景）；扩展 = 中小团队；痛点 = 大厂工具太重、主流工具不把 agent 当员工
- **价值评估**：轻量 RICE 变体，S/M/L/废弃 四档；评估框架**完全自定义**（M3）
- **估时双轨**：任务估时随认领者切换（人→人工工时，Agent→Agent 工时）
- **技术栈**：TypeScript 全栈前后端分离（React + NestJS + PostgreSQL + Redis + 自建 AI 流水线状态机 + Docker）

### 里程碑（共 91 叶子功能；v0.7 新增 TE-12/13、IT-10，v0.9 新增 VE-01/02，v0.13 定稿修订新增 RE-19/NT-04）
| 里程碑 | 主题 | 项数 |
|---|---|---|
| M0 | 领域地基（无 AI）：空间/账号/成员/空间角色权限（7 类人类角色 + 12 类 Agent）+ 三层需求树 CRUD + 版本规划 + 进度汇总 + 发布单 + 任务双类型看板（开发 6 态/测试 4 态）+ 验收打回 + 测试打回（升架构师）+ 废弃（级联+恢复）+ 租户隔离 + 前端骨架 + Dashboard | 39 |
| M1 | AI 流水线：L1 澄清 + L2 评估/PRD/评审 + L3 设计/评审/拆分（开发+测试任务）+ UI 设计（PRD 定稿后启动）+ 测试计划/用例/报告（占位），含 Agent 执行与草稿+确认 | 29 |
| M2 | AI 管理闭环：迭代自动装填 + AI 分派 + 甘特/负载图 + 提醒 + 副作用确认 + 拆分质量反馈 + CI/CD 自动发布 | 14 |
| M3 | 打磨：自定义评估框架、报表、运营/客服后台 | 9 |

---

## 待办

- [x] **确认 PM 留的 3 点**（2026-08-06 完成）：①L3 设计定稿确认人按来源分（L2 拆的业务 L3 产品经理确认、直建技术 L3 架构师确认）②测试任务计入 L3 进度（确认含）③提出人线上验收（后置，按所提层触发，不阻塞完成）
- [ ] **PRD v0.13 业务评审收尾**：用户通读 v0.13 定稿
- [ ] **PRD 定稿后**：同步 DDD v0.4 → v0.8~v0.13 口径（任务双类型、测试环节、角色重构、版本补充、验收状态机、Release 聚合对齐）
- [ ] **M0 技术设计**（架构师）：表结构 + API 契约 + 项目脚手架 + M0 任务排期
- [ ] 待定技术问题（见 `docs/requirements/07-开放问题.md` 仍待定 + `docs/README.md` §5 第 8~12 项）：分派超载一致性、进度投影时延/幂等、分解一致性、规模自适应边界、运营只读例外
- [ ] 通读 PRD v0.12 确认业务基线（用户侧）

---

## 关键文档
- **PRD（多文件）**：`docs/requirements/`（README.md 为索引）
  - 03-需求全生命周期流程详解.md（谁×节点×动作×输出 大章，含 §3.1.0 脑图 + 任务双类型图）
- **DDD 领域模型**：`docs/architecture/DDD-领域模型.md`（v0.4，**停在 v0.7 口径，PRD 定稿后待同步 v0.8~v0.12**）
- **整合索引**：`docs/README.md`（v0.12）

---

## 明日续接
直接对 Leader 说「继续需求管理平台项目」，读取本文件 + `docs/requirements/README.md` + `.claude/memory/` 恢复上下文。
**优先议题**：①确认 PM 回填 v0.12 后留的 3 点（L3 设计定稿确认人 / 测试任务计入 L3 进度 / 提出人反馈口子）→ ②PRD v0.12 业务评审收尾 → 定稿 → 同步 DDD → M0 技术设计。

---

## 六角色评审 grilling 定稿修订清单（2026-08-12）

**评审**：PM/前端/UX/UI/后端/测试 六角色联合评审 v0.13，4 BLOCKER + 15 MAJOR + 11 MINOR，逐项 grill 用户确认。以下为已确认口径，回填 PRD 按此执行（PM 回填 → 后端/测试复核 → 定稿）。

### 已确认决策（用户拍板）

| # | 决策 | 口径 |
|---|---|---|
| B1 | 验收单内轻量对话流 | 「已验收」交互承载 = 需求验收单（挂需求详情、不新增页面）：功能清单 + 验收项 + 上线链接 + 通过/异议动作(附原因) + 单内对话（异议→产品经理回应：接受返工/说明理由请撤回；留痕=对齐记录）。一人多角验收只执行一次 |
| B2 | 提出人可见性=权限并集 | 有效可见范围 = 提出人基线视图 ∪ 空间角色权限；「仅见自己需求/不可见迭代任务成员」仅限无空间角色提出人；角色授予只增不减（被赋产品经理 → 全量可见） |
| B3 | 父级「已验收」= 全部直接子级已验收 | 父级待验收→已验收前置 = 全部直接子级均已「已验收」；「已验收」为真终态、不可回落；版本「已交付」天然不可逆（消解二次验收/已交付回退） |
| B4 | 强制人工=旁路标记 | 评审/验收打回累计 3 次 → 需求挂「强制人工」标记，状态机不变（仍回原流转），该环节 AI 不代行、路由人工，人工处理通过后解除；RE-05 断言改「标记出现 + 路由转人工」 |
| M1 | 走查补解除走通全流程 | B1 统一作用于所有边界（含 L3→任务：挂起/阻塞/进行中的直接子级阻止父级开发完成）；步13挂起两任务（误判兜底队列/分类报表）→ 两 L3 均保持进行中 → 发布单#1=4+#2=1（发5活动任务）→ 新增15b 挂起解除 → 发布单#3=2 → 7任务全完成 → L3→L2→L1 自底向上开发完成 → 验收段完整演示 |
| M4 | 轻量待办清单入 M0 | 「我的待办/决策队列」按角色聚合（待确认档位/待评审裁决/待三方确认/待验收/返工待办/异议回应）；返工待办实体 = 待办清单项；与 M5 最小通知同批 M0 |
| M5 | 最小通知入 M0 | 进入待验收最小站内通知（红点/待验收清单高亮）划入 M0 作验收闭环载体；NT-01 完整通知中心留 M1；NT-02 优先级催办 + AI-08 留 M2 |
| M6 | 废弃恢复=快照回滚 | 恢复 = 整棵子树回滚至废弃前状态快照；已废弃子级保持废弃可单独恢复、已完成任务保持已完成；留痕「已恢复(回滚至废弃前)」；v0.8「回最初状态」改为「回废弃前状态」 |
| M9 | 催办锚点=进入待验收时刻 | 第0天进入即通知；满 N 天未敲定 → 每 N 天催办一次直到敲定；无动作满 3 轮 → 升级空间负责人人工介入（可配置）；验收仍人工专属、系统不自动裁决 |
| M10 | 补纠错回退规则 | §3.1.5 补「人工强制回退(纠错)」：产品经理/空间负责人（权限 TE-04）可将任意需求/任务强制回退到指定合法前置态；必填原因留痕；回退≠打回、不计打回计数、不触发升人工 |
| M12 | UI 设计=正式产物硬输入 | L2 必产 UI 设计（有 UI 交互必产、纯后端 L3 标注「无 UI」可免），作 L3 设计+测试拆分硬输入；补产物验收标准（PRD 功能清单 + a11y 基线 + 状态-色彩映射一致）；评审打回→PRD修订→UI 联动修订；存储挂 RE-14 附件 |

### 技术口径（Leader 定，随回填落地）

- **M2 验收子状态**：需求落库 `pmDecision`(产品经理裁决) + `proposerConfirmation`(提出人确认，仅提出层)；已验收守卫按「是否提出层」校验组合 + 全部直接子级已验收前置
- **M3 父级回落一致性**：需求表物化 `derived_state` + 直接父级增量冒泡重算；进度 = 状态派生值（待验收/已验收=100%，统一「状态与进度一致」表述）
- **M7 测试任务打回**：测试任务执行用例失败 → 回「待执行」+ 失败标记，重测重走；三方确认前可改判；关联开发任务打回不变
- **M11 通知口径**：NT-01 类别枚举增补验收段（进入待验收通知/验收催办/返工待办/异议回应）；NT-02 补「P0/P1/P2/P3 催办频率映射可配置」
- **M13 状态-色彩映射表**：PRD 附一张表（各层状态→颜色/图标/进度语义），供定稿后 UI 设计直接使用
- **M14 跨端契约**：实时通道(轮询/SSE/WS)选型、进度/就绪度/已交付 API 契约、服务端下发 permissions、跨对象导航、处理人感知 → M0 技术设计阶段定，PRD 预留
- **M15 看板拖拽**：拖拽改状态遵循同一校验（打回必填原因弹表单）
- **N1 权限点拆分**：矩阵「评估裁决」拆「评估计算(PM Agent 子能力)」+「评估裁决(人工专属，AI 永不代行)」
- **N9 版本流转触发**：版本「规划中→进行中」= 产品经理手动标记（或首个需求归入时系统建议、人工确认）
- **新增叶子**：RE-19 需求验收单、NT-04 我的待办/决策队列 → 89→91，M0 含；README 变更说明注明

### 回填文件清单

1. **02-名词表.md**：增补验收单 / 强制人工(旁路标记) / 回退(纠错动作) / 待办·决策队列 / 验收子状态 / 废弃恢复(快照) 名词
2. **03-需求全生命周期流程详解.md**：§3.1.5 状态机（已验收守卫/回退迁移/强制人工标记）→ 例外 2/4/8/10/11（升人工标记、废弃快照恢复、验收子状态+验收单、催办锚点+升级）→ 新增「人工强制回退」例外 → 状态-色彩映射表 → §3.4.2 走查步 13/15/15a 重写 + 步 15b
3. **04-业务域与功能清单.md**：RE-05/06（验收守卫/强制人工标记/回退规则/挂起阻止完成）、NT-01（类别增补）、NT-02（优先级映射可配置）、新增 RE-19 需求验收单、NT-04 我的待办/决策队列、权限矩阵 N1 拆分、UI 设计产物承接（RE-12/13 附件或 RE-14）、版本流转触发 N9
4. **05-里程碑交付.md**：M0 增补 RE-15/NT-04/最小通知，叶子 89→91；v0.13 变更说明补本轮决策
5. **README.md**：v0.13 本次定稿修订段补本轮 grilling 决策（B1~B4、M1/M4/M5/M6/M9/M10/M12、叶子 89→91）
6. **06-共享理解覆盖核对.md / 07-开放问题台账.md**：相关 H 条目与待定问题同步

### 回填后复核（2026-08-12 后端/测试复核）
- **矛盾 A（B4 回填不完整，修）**：§3.1.5 合法流转 row 12「L2 评审中→强制升人工」、row 20e「验收打回累计3次→强制升人工」及脑图/流程/§3.1.2/§3.2.2/§3.2.3/§3.3/§3.5/02名词表多处仍用旧「强制升人工=状态/人工直接放行」模型，与「旁路标记、状态机不变」冲突 → 统一改旁路标记口径（仍回原流转、AI 不代行、人工处理该环节后解除）
- **矛盾 B（已确认，用户拍板）**：常规纠错回退排除终态——需求「已验收」、任务「已完成」不可常规回退；终态误设走空间负责人「数据修正」（高权限+二次确认+留痕，独立于常规回退）；版本「已交付」随数据修正回退「进行中」
- **MINOR 清理**：①03 §3.3 提出人绝对可见性表述（L608/623）补 B2「仅限无空间角色提出人」限定 ②05 v0.8 变更说明「回最初状态」补行内纠正「本次定稿修订改为回废弃前状态快照」 ③NT-04 依赖字段补「NT-01 的 M0 子集」防误判 M0 需全量通知中心 ④N9 版本「规划中→进行中」触发补进 03 §3.1.2 版本模型段 ⑤todo.md 叶子编号笔误「RE-15」应为「RE-19」
- **测试复核补充断言缺口（修）**：①RE-05 验收标准列补「需求级验收打回累计 3 次 → 挂强制人工旁路标记（路由人工、AI 不代行、处理通过后解除）」断言 ②TA-02 验收标准列补「测试任务执行用例失败 → 回『待执行』+ 失败标记（重测重走）、三方确认前可改判」断言 ③NT-02 验收标准列补「默认 P0=1/P1=2/P2=3/P3=7 可覆盖」映射值 ④M10 22a/例外 12 补「已验收/已完成除外，终态误设走数据修正」豁免声明（=矛盾 B，已确认）
- **待修复后** → 定稿 → 同步 DDD v0.4 → M0 技术设计
